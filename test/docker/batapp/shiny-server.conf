run_as shiny;

server {
    listen 3838;

    location / {
        app_dir /srv/shiny-server/batApp;
        log_dir /var/log/shiny-server;
        app_init_timeout 300;
        app_idle_timeout 0;
    }
}